# Geo Download
先读 README.md、HANDOFF.md、config.yaml。以磁盘任务清单交接，不依赖历史对话。
本项目与主线学习仓库独立；不要修改学习笔记或继承其 Git 自动推送授权。
只处理用户明确选中的输入。用户当前搁置夸克，禁止运行云端上传或清理。
使用 sat 环境，不改全局环境。修改后运行 python -m unittest discover -s tests -v。
